#include <stdio.h>
/**
 * main - entry point
 * Return: return 0
 */
int main(void)
{
	int a;
	int b;

	for (a = 97; a <= 122; a++)
	{
		putchar(a);
	}
	for (b = 65; a <= 90; b++)
	{
		putchar(b);
	}
	putchar('\n');
	return (0);
}
